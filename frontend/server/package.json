{
  "name": "lehackit-server",
  "version": "0.1.0",
  "private": true,
  "type": "commonjs",
  "scripts": {
    "dev": "npm run build && node dist/index.js",
    "build": "tsc -p tsconfig.json",
    "start": "node dist/index.js",
    "test": "npm run build && vitest run --coverage",
    "test:watch": "vitest",
    "test:unit": "vitest run test/*.test.ts --coverage"
  },
  "dependencies": {
    "multicast-dns": "^7.2.5",
    "protobufjs": "^7.5.4",
    "vigemclient": "^1.5.3"
  },
  "devDependencies": {
    "@types/multicast-dns": "^7.0.4",
    "@types/node": "^22.15.3",
    "@vitest/coverage-v8": "^2.1.8",
    "typescript": "^5.9.3",
    "vitest": "^2.1.8"
  }
}
